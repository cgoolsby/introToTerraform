Terraform Files to create 4 nodes (m4large) in the cloud, then remote exec and install spark on each one.  

Please generate your own key and key.pub with the command:
```ssh-keygen -t rsa -f key -p ''```

